[package]
name = "lambert_izzo_test_support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Workspace-internal dev fixtures for lambert_izzo (constants, RNG helpers, Kepler propagator, random LambertInput batches)."
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
lambert_izzo = { workspace = true }
rand = "0.8"
rand_chacha = "0.3"
