[package]
name = "lambert_izzo_test_support"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Workspace-internal dev fixtures for lambert_izzo (constants, RNG helpers, Kepler propagator)."
license.workspace = true
publish = false

[dependencies]
rand = "0.8"
